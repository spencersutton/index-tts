[project]
name = "index-tts-vllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "vllm==0.10.2",
    "descript-audiotools==0.7.2",
    "matplotlib==3.8.2",
    "omegaconf",
    "sentencepiece",
    "librosa",
    "gradio",
    "ninja",
    "modelscope",
    "munch==4.0.0",
    "loguru",
    'WeTextProcessing; platform_machine != "Darwin"',
    'wetext; platform_system == "Darwin"',
]

[tool.ruff]
line-length = 120
preview = true

[tool.ruff.lint]
extend-select = ["E", "F", "W", "C90", "B", "B9", "I", "Q0", "TID251", "TID252"]
