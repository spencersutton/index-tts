import torch
import torch.nn as nn

class VectorQuantize(nn.Module):
    def __init__(self, input_dim: int, codebook_size: int, codebook_dim: int) -> None: ...
    def forward(self, z):  # -> tuple[Any, Tensor, Tensor, Tensor, Any]:
        ...
    def embed_code(self, embed_id):  # -> Tensor:
        ...
    def decode_code(self, embed_id):  # -> Tensor:
        ...
    def decode_latents(self, latents):  # -> tuple[Tensor, Tensor]:
        ...

class ResidualVectorQuantize(nn.Module):
    def __init__(
        self,
        input_dim: int = ...,
        n_codebooks: int = ...,
        codebook_size: int = ...,
        codebook_dim: int | list = ...,
        quantizer_dropout: float = ...,
    ) -> None: ...
    def forward(
        self, z, n_quantizers: int = ...
    ):  # -> tuple[Any | Literal[0], Tensor, Tensor, Any | Literal[0], Any | Literal[0]]:
        ...
    def from_codes(self, codes: torch.Tensor):  # -> tuple[float | Any, Tensor, Tensor]:
        ...
    def from_latents(self, latents: torch.Tensor):  # -> tuple[Any | Literal[0], Tensor, Tensor]:
        ...

if __name__ == "__main__":
    rvq = ...
    x = ...
    y = ...
