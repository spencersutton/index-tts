from collections import UserDict
from dataclasses import dataclass

import fastapi

OAUTH_CLIENT_ID = ...
OAUTH_CLIENT_SECRET = ...
OAUTH_SCOPES = ...
OPENID_PROVIDER_URL = ...
MAX_REDIRECTS = ...

def attach_oauth(app: fastapi.FastAPI):  # -> None:
    ...

@dataclass
class OAuthProfile(UserDict):
    name: str = ...
    username: str = ...
    profile: str = ...
    picture: str = ...
    def __init__(self, data: dict) -> None: ...

@dataclass
class OAuthToken:
    token: str
    scope: str
    expires_at: int
