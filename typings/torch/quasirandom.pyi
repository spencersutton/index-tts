import torch

class SobolEngine:
    MAXBIT = ...
    MAXDIM = ...
    def __init__(self, dimension, scramble=..., seed=...) -> None: ...
    def draw(
        self,
        n: int = ...,
        out: torch.Tensor | None = ...,
        dtype: torch.dtype | None = ...,
    ) -> torch.Tensor: ...
    def draw_base2(
        self,
        m: int,
        out: torch.Tensor | None = ...,
        dtype: torch.dtype | None = ...,
    ) -> torch.Tensor: ...
    def reset(self) -> Self: ...
    def fast_forward(self, n) -> Self: ...
