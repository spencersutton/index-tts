"""
This type stub file was generated by pyright.
"""

import os
from typing import BinaryIO, Optional, Tuple, Union

import torch
import torchaudio

from .backend import Backend
from .common import AudioMetaData

InputType = Union[BinaryIO, str, os.PathLike]

def info_audio(src: InputType, format: str | None, buffer_size: int = ...) -> AudioMetaData: ...
def load_audio(
    src: InputType,
    frame_offset: int = ...,
    num_frames: int = ...,
    convert: bool = ...,
    channels_first: bool = ...,
    format: str | None = ...,
    buffer_size: int = ...,
) -> tuple[torch.Tensor, int]: ...
def save_audio(
    uri: InputType,
    src: torch.Tensor,
    sample_rate: int,
    channels_first: bool = ...,
    format: str | None = ...,
    encoding: str | None = ...,
    bits_per_sample: int | None = ...,
    buffer_size: int = ...,
    compression: torchaudio.io.CodecConfig | None = ...,
) -> None: ...

class FFmpegBackend(Backend):
    @staticmethod
    def info(uri: InputType, format: str | None, buffer_size: int = ...) -> AudioMetaData: ...
    @staticmethod
    def load(
        uri: InputType,
        frame_offset: int = ...,
        num_frames: int = ...,
        normalize: bool = ...,
        channels_first: bool = ...,
        format: str | None = ...,
        buffer_size: int = ...,
    ) -> tuple[torch.Tensor, int]: ...
    @staticmethod
    def save(
        uri: InputType,
        src: torch.Tensor,
        sample_rate: int,
        channels_first: bool = ...,
        format: str | None = ...,
        encoding: str | None = ...,
        bits_per_sample: int | None = ...,
        buffer_size: int = ...,
        compression: torchaudio.io.CodecConfig | float | int | None = ...,
    ) -> None: ...
    @staticmethod
    def can_decode(uri: InputType, format: str | None) -> bool: ...
    @staticmethod
    def can_encode(uri: InputType, format: str | None) -> bool: ...
