from PIL import Image

from ..utils import is_torch_available, is_vision_available
from .base import Pipeline
from .pt_utils import KeyDataset

if is_vision_available(): ...
if is_torch_available(): ...
logger = ...

class VisualQuestionAnsweringPipeline(Pipeline):
    _load_processor = ...
    _load_image_processor = ...
    _load_feature_extractor = ...
    _load_tokenizer = ...
    _pipeline_calls_generate = ...
    _default_generation_config = ...
    def __init__(self, *args, **kwargs) -> None: ...
    def __call__(
        self,
        image: Image.Image | str | list[Image.Image] | list[str] | KeyDataset,
        question: str | list[str] | None = ...,
        **kwargs,
    ):  # -> list[Any] | PipelineIterator | Generator[Any, Any, None] | Tensor | Any | None:

        ...
    def preprocess(self, inputs, padding=..., truncation=..., timeout=...):  # -> BatchEncoding | Any:
        ...
    def postprocess(self, model_outputs, top_k=...):  # -> list[dict[str, str | Any]] | list[dict[str, Any | str]]:
        ...
