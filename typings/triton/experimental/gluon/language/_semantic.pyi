"""
This type stub file was generated by pyright.
"""

from typing import Sequence, TypeVar

from triton._C.libtriton.gluon_ir import GluonOpBuilder
from triton.language.semantic import TritonSemantic

from . import _core as ttgl

TensorTy = TypeVar("TensorTy")

class GluonCallerContext:
    def __init__(self, num_warps: int) -> None: ...
    def mangle(self):  # -> str:
        ...
    def initialize_callee(self, fn, builder):  # -> None:
        ...

class GluonSemantic(TritonSemantic[TensorTy]):
    tensor = ...
    lang = ttgl
    builder: GluonOpBuilder
    def __init__(self, builder: GluonOpBuilder) -> None: ...
    def expand_dims(self, input: TensorTy, axis: int) -> TensorTy: ...
    def join(self, a: TensorTy, b: TensorTy) -> TensorTy: ...
    def split(self, a: TensorTy) -> tuple[TensorTy, TensorTy]: ...
    def permute(self, input: TensorTy, dims: tuple[int]) -> TensorTy: ...
    def broadcast_impl_shape(self, input: TensorTy, shape: tuple[int]) -> TensorTy: ...
    def broadcast_impl_value(self, lhs: TensorTy, rhs: TensorTy) -> TensorTy: ...
    def arange(self, start, end, layout):  # -> TensorTy:
        ...
    def reshape(self, input: TensorTy, dst_shape: list[int], can_reorder: bool):  # -> TensorTy:
        ...
    def splat(self, value, shape, layout):  # -> tensor:
        ...
    def full(self, shape, value, dtype, layout):  # -> tensor:
        ...
    def convert_layout(self, value, layout, assert_trivial=...):  # -> tensor:
        ...
    def allocate_shared(self, element_ty, shape, layout, value):  # -> shared_memory_descriptor:
        ...
    def shared_load(self, mem_desc, layout):  # -> tensor:
        ...
    def shared_store(self, mem_desc, value):  # -> None:
        ...
    def shared_dealloc(self, mem_desc):  # -> None:
        ...
    def set_auto_layout(self, value, layout):  # -> TensorTy:
        ...
    def memdesc_slice(self, mem_desc, start, length, dim):  # -> shared_memory_descriptor:
        ...
    def memdesc_index(self, mem_desc, index):  # -> shared_memory_descriptor:
        ...
    def memdesc_trans(self, mem_desc, order):  # -> shared_memory_descriptor:
        ...
    def memdesc_reshape(self, mem_desc, shape):  # -> shared_memory_descriptor:
        ...
    def memdesc_reinterpret(self, mem_desc, dtype, shape, layout):  # -> shared_memory_descriptor:
        ...
    def wrap_tensor(self, x, scalar_ty, ret_shape, layout):  # -> TensorTy:
        ...
    def associative_scan(
        self, inputs: Sequence[TensorTy], axis: int, region_builder_fn, reverse: bool
    ) -> tuple[TensorTy, ...]: ...
    def reduction(self, inputs: Sequence[TensorTy], axis: int, region_builder_fn) -> tuple[TensorTy, ...]: ...
    def histogram(self, input: TensorTy, num_bins: int, mask: TensorTy, layout) -> TensorTy: ...
    def warp_specialize(
        self,
        default_args,
        default_partition,
        worker_args,
        worker_partitions,
        worker_num_warps: Sequence[int],
        worker_num_regs: Sequence[int],
        generator,
    ):  # -> tuple[base_value, ...] | None:
        ...
