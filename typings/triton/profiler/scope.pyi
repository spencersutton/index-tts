"""
This type stub file was generated by pyright.
"""

from typing import Union

thread_local_scopes = ...
type MetricValueType = Union[float, int]

class scope:
    """
    A context manager and decorator for entering and exiting a scope.

    Usage:
        context manager:
        ```python
        with proton.scope("test0", {metric_name: metric_value}):
            foo[1,](x, y)
        ```

        decorator:
        ```python
        @proton.scope("test0", {metric_name: metric_value})
        def foo(x, y):
            ...
        ```

    Args:
        name (str): The name of the scope.
        metrics (dict[str, float], optional): The metrics of the scope. Default is None.
    """
    def __init__(self, name: str, metrics: dict[str, MetricValueType] | None = ...) -> None: ...
    def __enter__(self):  # -> Self:
        ...
    def __exit__(self, exc_type, exc_value, traceback):  # -> None:
        ...
    def __call__(self, func):  # -> _Wrapped[..., Any, ..., Any]:
        ...

class cpu_timed_scope(scope):
    """
    A scope that measures elapsed time (cpu_time).

    Args:
        name (str): The name of the scope.
        metrics (dict[str, float], optional): Additional metrics to add. Default is None.
    """
    def __init__(self, name: str, metrics: dict[str, float] | None = ...) -> None: ...

def enter_scope(name: str, *, metrics: dict[str, MetricValueType] | None = ...) -> int | None: ...
def exit_scope(name: str | None = ..., *, metrics: dict[str, MetricValueType] | None = ...) -> int | None: ...
